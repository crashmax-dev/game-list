<Box>
              <Image src={property.imageUrl} onClick={onOpen} />
              <>
                <Modal isOpen={isOpen} onClose={onClose} isCentered>
                  <ModalOverlay />
                  <ModalContent>
                    <ModalHeader>{property.title}</ModalHeader>

                    <ModalBody>
                      <Box>
                        <Image src={property.imageUrl}></Image>
                      </Box>
                      <Box>{property.description}</Box>
                    </ModalBody>

                    <ModalFooter>
                      <Button colorScheme='blue' mr={3} onClick={onClose}>
                        Закрыть
                      </Button>
                      <Button variant='ghost'>Ссылка в магазин</Button>
                    </ModalFooter>
                  </ModalContent>
                </Modal>
              </>
              <Box>
                {property.title}
              </Box>
              <Box >
                {property.rating}
              </Box>
            </Box>